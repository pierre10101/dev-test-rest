// Since I test this in the router.test.js file and this is a dev test I am not going to write these.
describe('Future test for controller', () => {
    it('returns 200', async () => {
        expect(200).toEqual(200);
    })
})